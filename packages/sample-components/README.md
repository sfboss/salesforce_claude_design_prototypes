# sample-components

Reference LWC bundles produced by `pnpm generate:demo`. Run that command to populate
`force-app/main/default/lwc/` with the canonical Account Intelligence variants. Files
under this folder mirror what would land in a Salesforce DX project.
